import "dotenv/config";

// This file exists to ensure local `.env` files are loaded before other server modules
// evaluate `process.env.*` reads. On Replit, env vars come from "Secrets" instead.



