node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Local state and binaries
.local/
cloudflared

# Local secrets (Replit Secrets do not download with the repo)
.env
.env.*

# Staff photo sync — machine-local venv, downloads, logs, run state.
# (The processed photos in "Website Pictures Lexington/staff" and names.json
#  ARE committed; only these local artifacts are ignored.)
scripts/staff-sync/.venv/
scripts/staff-sync/staging/
scripts/staff-sync/sync.log
scripts/staff-sync/state.json
scripts/staff-sync/__pycache__/